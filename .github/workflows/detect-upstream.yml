name: detect-upstream
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          path: builder
          ref: main
      - id: cmp
        run: |
          cd builder
          OUT=$(python3 tools/check-upstream.py plain)
          echo "$OUT"
          echo "$OUT" | grep -q "^CHANGED " && echo "changed=true" >>"$GITHUB_OUTPUT" || echo "changed=false" >>"$GITHUB_OUTPUT"
      - name: Mirror changed branches
        if: steps.cmp.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.KERNEL_MIRROR_TOKEN }}
        run: |
          cd builder
          python3 tools/mirror-upstream.py plain --push
      - name: Dispatch plain fleet
        if: steps.cmp.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run build-kernel.yml --ref main -f variant=plain -f destination=artifact -f branch=all
