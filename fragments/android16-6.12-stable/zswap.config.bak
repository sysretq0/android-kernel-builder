# zswap compressed swap cache (per-branch: KMI allowlist scoping).
# Status on this tree: UNPROVEN (prior run failed on netfs/zsmalloc
# before the question was reached). Pool backend rides alongside
# (zbud.config). No frontswap line (deleted upstream on 6.6+).
# Compressor default left stock; chosen at runtime (CRYPTO_ZSTD=y).
CONFIG_ZSWAP=y
