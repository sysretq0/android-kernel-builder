# USB mass-storage gadget (expose phone storage over USB).
# Deps: USB_CONFIGFS (effective: UAC2/MIDI functions are on) + BLOCK.
# Note the symbol name: USB_CONFIGFS_MASS_STORAGE (no _F_ infix, unlike the
# UAC2/MIDI function symbols).
CONFIG_USB_CONFIGFS_MASS_STORAGE=y
