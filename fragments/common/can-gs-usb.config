# candleLight USB-CAN (gs_usb) for car-hacker tooling from the phone.
# Needs USB + CAN (CAN=m stock, vendor modules shipped); no Kconfig gates,
# undeclared in every modules list. Dormant without the dongle.
# Verified 5.10..6.18: zero exports, zero module-list entries.
CONFIG_CAN_GS_USB=y
