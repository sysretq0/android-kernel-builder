# CAKE queue management (bufferbloat fight for hotspot/tethering).
# Pairs with BBR (pacing + fq/cake is the recommended combo). Standalone
# tristate, no deps; new name so no shipped .ko to conflict with.
CONFIG_NET_SCH_CAKE=y
