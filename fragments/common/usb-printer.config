# USB printer (usblp) for USB printing from the phone.
# Dep-less tristate, no Kconfig gates in usb/class (nothing else unhidden),
# dormant without a printer. =y because we ship Images, not .ko files.
# Verified 5.10..6.18: zero exports, zero module-list entries.
CONFIG_USB_PRINTER=y
