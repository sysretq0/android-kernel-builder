# FRONTSWAP (zswap prerequisite on pre-6.6 trees only).
# 5.10/5.15/6.1: ZSWAP depends on (or selects) FRONTSWAP, so spell it.
# 6.6+: symbol deleted upstream (frontswap removed) -- must NOT be listed
# (unknown =y fails the per-symbol check). Hence per-branch, not common/.
CONFIG_FRONTSWAP=y
